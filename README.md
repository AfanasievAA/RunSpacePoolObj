This project is a historical artefact from the pre‑AI era — when humans wrote and tested code by hand, bugs and all. It mostly works, but consider it a monument to software archaeology. Please don't deploy this in production — unless you're running a retro‑computing museum. )))
# RunSpacePoolObj
Powershell object used to easy run/track parralel processes with RunSpacePool technique. 
Allows you to easily run, track powershell scriptblocks in parralel with timeout feature and return values
